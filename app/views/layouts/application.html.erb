<!DOCTYPE html>
<html>
  <head>
    <title>DISASTER FORUM MAKATI</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", defer: false %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: false %>
  </head>

  <body>
  <%= render 'home/header' %>
  <p class="alert-danger"><%= notice %></p>
  <p class="alert-danger"><%= alert %></p>
  <div class="container border">
  <%= yield %>
  </div>
  <%= render 'home/footer' %>
  </body>
</html>
